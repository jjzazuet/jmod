apply plugin: 'groovy'

dependencies {
   compile project(":fmod-${osName}-${osArch}")
   compile project(":fmod-studio-${osName}-${osArch}")
   compile "junit:junit:4.12"
   compile "org.spockframework:spock-core:[1.0-groovy-2.4,)"
}
